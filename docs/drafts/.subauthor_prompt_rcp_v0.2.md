You are the **4.6 sub-author** in KairosChain's design revision protocol. The 4.7
orchestrator will integrate your output with a reject log. Your job: produce **v0.2**
of the design-by-invariant draft below, addressing review findings B1–B8, while
preserving the register and KairosChain's philosophy.

## Hard constraints (do not violate)

1. **Design-by-invariant.** State invariants + a one-line justification. Do NOT
   enumerate mechanisms. No method names, file paths, table/column names, API/payload
   shapes, consumer names, hook names, or line numbers. Mechanism choices belong in the
   backlog section only.
2. **Anti-enumeration / consolidation.** The v0.1 had 10 labelled invariants; a reviewer
   flagged that as heavy. PREFER CONSOLIDATING over adding many new labelled invariants.
   Fold new requirements into existing invariants where the meaning allows. Target: do
   not exceed ~10–12 invariants total.
3. **KairosChain philosophy is the gating frame**, not generic engineering pragmatism:
   - Prop 5: recording is constitutive (blockchain), not evidential. Influence on the
     system's being must be recorded.
   - Prop 6: incompleteness is the driving force, not a defect.
   - Prop 10: any norm/claim must be contestable/revisable from within.
   - Knowledge Ethos: suspend contradictions (epoche), do not force resolution.
   - LLM bias awareness must be reflexive (the classifier is inside the frame).
   - No centralized control (P2P-natural).
   - merely-recursive vs structural self-reference: this capability is metacognitive
     MEASUREMENT, not structural self-referentiality; do not claim the latter.
4. **Output ONLY the revised markdown** for v0.2. No preamble, no commentary.

## Findings to address (these are genuine philosophy-aligned (b) findings)

- B1 (Inv-1 substrate). v0.1 says the profile is a pure function of the "constitutive
  record", but the (a)/(b)/(c) classifications + dispositions may today live in an
  evidential/volatile L2 log, not the blockchain-recorded layer. Resolve by making it an
  invariant precondition: the substrate the profile reads must ITSELF be constitutively
  recorded; data that is not constitutively recorded is not eligible substrate.
- B2 (Inv-2/Inv-6 seam). The case where the profile informs a human who then edits L0/L1
  can slip through unrecorded. Resolve: any influence reaching an L0/L1-affecting
  decision — direct OR human-mediated — falls under the recording requirement.
- B3 (insufficient data). Under sparse/cold-start history the derivation emits confident-
  looking low-n noise. Add the requirement: under insufficient record the profile must
  declare insufficiency rather than emit a value.
- B4 (Inv-9 unfalsifiable). "Must not bias toward premature APPROVE/convergence" has no
  observable. Make it checkable: the capability's effect on convergence must be
  observable against the recorded review history, so the invariant can be evaluated
  against the experiment's revert criteria.
- B5 (Inv-7 circularity). When the same agent both produces the (a)/(b)/(c) classification
  AND derives the profile, the derivation is circular. Bound it: such a derivation must
  not be represented as independent of the classification it consumes.
- B6 (Inv-4 implicit downweighting). Surfacing + "holding" a contradiction is itself a
  nudge and can soft-bypass the surfacing-not-deciding and anti-convergence invariants.
  Add: surfacing a held contradiction must be presentation-symmetric — it must not direct
  the orchestrator toward accepting or rejecting the present finding. Holding is not
  nudging.
- B7 (Inv-10 Meeting Place). Cross-instance skill/knowledge exchange could leak reviewer
  identity/profile history. Extend locality: reviewer identity and profile history must
  not cross the instance boundary without explicit recorded opt-in; default is
  non-export.
- B8 (Inv-5 vs Inv-1 contradiction). If a human can override a profile value, the profile
  is no longer a "pure function of the record" (Inv-1). Reconcile WITHOUT eliminating
  either pole (Knowledge Ethos): a human override is itself a recorded event that becomes
  new input the derivation consumes — so Inv-1 holds (override enters the record) and
  Inv-5 holds (override is possible). State this reconciliation explicitly.

Advisory (c) findings — do NOT add mechanism to satisfy them; they demand API/payload/
taxonomy detail that the design-by-invariant register defers to backlog. Leave in backlog.

## v0.1 draft to revise

# Reviewer Calibration Profile — Stage 1 Design (v0.1 draft)

**Status:** design draft, v0.1. Under multi-LLM review.
**Scope:** L1 capability design (metacognitive measurement layer over the existing
multi-LLM review workflow). NOT core/L0 infrastructure.
**Register:** design-by-invariant.

## Provenance
Adapted from the "Hindsight calibration" concept observed in an external personal-AI
harness (gbrain), reduced to a thin first stage. Adopts the measurement/surfacing idea;
rejects the source's resolve-oriented contradiction handling and value-neutral framing.

## 1. Intent
A capability that derives, per multi-LLM reviewer, a calibration profile from the
existing constitutive record: recorded history of past multi-LLM reviews (per-finding
(a)/(b)/(c) classification and verdicts) plus subsequently recorded dispositions (what
was internalized into L0/L1, revised, or shipped). The profile characterizes, per
reviewer and per section-register, the historical distribution of that reviewer's
findings across (a) deployment-grounded, (b) philosophy-aligned, (c) value-divergent,
and their eventual disposition. The profile is surfaced to the orchestrator at
review-aggregation time as calibration metadata. (masa mode currently hand-writes
reviewer-bias characterizations; stage 1 turns the hand-written profile into a read of
the record.)

## 2. Non-goals
- Not an automated gate; does not change any finding's blocking status.
- Not a port of the source system's typed-claim substrate; no new primary data store.
- Not a resolver of disagreement; never dismisses or downweights automatically.
- Not a forecasting/scoring engine in this stage.
- Not a new evidential side-channel competing with the blockchain record.

## 3. Invariants
- Inv-1 (Derived-from-record): pure function of the existing constitutive record; no new
  primary data store.
- Inv-2 (Surfacing-not-deciding): may inform interpretation; may never by itself alter a
  finding's blocking status or P0 count.
- Inv-3 (Calibration-not-dismissal): a profile value is a statement about historical
  tendency in a register, never a verdict on a present finding; (c)-prone in one register
  stays (a)-capable in another.
- Inv-4 (Held-disagreement): when a present finding contradicts the profile, surface and
  hold, do not resolve.
- Inv-5 (Contestable-profile): every value is provisional, surfaceable, revisable, carries
  provenance, overridable by a recorded human decision.
- Inv-6 (Recorded-influence): any instance where a profile value influences an
  L0/L1-affecting decision is itself recorded.
- Inv-7 (Reflexive-bias): derivation depends on prior (a)/(b)/(c) labels produced by a
  biased LLM; represent as bounded confidence, not ground truth.
- Inv-8 (Incompleteness): never complete/final; a moving read.
- Inv-9 (No-convergence-bias): surfacing must not bias the loop toward premature APPROVE.
- Inv-10 (Locality): derivation is local to the instance's own record; no global reviewer
  authority without explicit recorded opt-in.

## 4. Philosophical consistency map
(maps each Prop / Knowledge Ethos / bias-awareness to the invariants above; includes a
"merely-recursive honesty" note that this is metacognitive measurement, not structural
self-reference.)

## 5. Backlog (deferred mechanism choices)
- profile representation/format; where derivation runs; register/section taxonomy
  granularity; later-stage forecast; interaction with the Anthropic persona unanimity
  gate; confidence representation.
