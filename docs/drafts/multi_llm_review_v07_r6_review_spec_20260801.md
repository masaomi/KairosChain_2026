# v0.7 R6 レビュー仕様（事前宣言・差分のみ）— 受理された集合の上での測定

- 日付: 2026-08-01
- 状態: DECLARED（R6 投函前に宣言。投函後は変更しない）
- 土台: `docs/drafts/multi_llm_review_v07_review_spec_20260731.md`＋R2〜R5 差分 spec。本書は R5 からの差分のみ
- R5 の結果: 機械集計は初の APPROVE（3/4・閾値到達）だが、codex の APPROVE は 41 文字空返答型のため土台 spec §3 により数えない。実質 APPROVE = claude_cli（2,427 字・5 ラウンド連続）＋cursor（1,655 字・2 ラウンド連続）。persona 座席は REVISE — ただし当日セッション内の内訳は Guardian APPROVE・Pragmatist APPROVE・Skeptic REVISE で、現行機構の優先順位合成が 1 行に潰した結果（本設計 INV-R3 が変える挙動の実演。個別出力は記録に残らない）。P0 生数 20 → 11 → 9 → 6 → **1**。(b) は 4 ラウンド連続ゼロ

## 1. 修正（1 件）と、この修正が新しく主張すること

| # | 修正 | この修正が新しく主張すること |
|---|---|---|
| K1 | 受理された集合の上での測定（INV-R2/R4） | INV-R2 の「行の集合」= **受理の門（INV-R1）を通った行の集合**と定義。差し戻しはそれ自体が母数の事由の位置を占めることはなく、母数への影響がありうるとすれば受理された集合の変化を経由する INV-R2 の変動としてであり、経緯は記録から読める。現行の粒度（呼び出し全体）では差し戻し時点で受理された集合が構成されないため母数は動かない — 無条件の中立性主張を、測定経由の条件つき記述へ置き換え |

## 2. R6 で advisory（(c)・queue）扱いのもの

R5 までの (c)/P3 を引き継ぐ（体数下限の 2 箇所連動・空集合 assemble の APPROVE 落ち・受理点の位置・体数の超過方向・語彙の第 3 の場・承継の地図・「無応答の確定」の一段実行での言い回し・二事由の注記の数え方 等 — すべて §6 backlog か実装時注記に置き場がある）。宣言済み Open Unknown 2 件（批准 2026-07-31）の再指摘は (c)。空返答型 APPROVE は数えない。

## 3. 合格条件・閉じ方（変更なし）

土台 spec §2〜§4 のとおり: (a) ゼロ＋(b) 消尽で合格、(c) は advisory。閾値 3/5 は参考値、主経路は (a)+(b) 消尽 → masaomi の freeze 宣言。配布は枠ごと（claude_cli = 本文込み）。collect 期限 5400 秒（override 明示）。反証専任を投函前に 1 体。
