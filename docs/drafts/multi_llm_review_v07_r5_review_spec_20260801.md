# v0.7 R5 レビュー仕様（事前宣言・差分のみ）— 実質の測り方を現行の広さへ戻す

- 日付: 2026-08-01
- 状態: DECLARED（R5 投函前に宣言。投函後は変更しない）
- 土台: `docs/drafts/multi_llm_review_v07_review_spec_20260731.md`＋R2〜R4 差分 spec。本書は R4 からの差分のみ
- R4 の結果: 集計 REVISE。実質 APPROVE 2 票（claude_cli 2,659 字・4 ラウンド連続／**cursor 833 字・R1〜R3 REVISE から転換**）。codex は空返答型から復帰し 497 字の実質 REVISE。persona 座席は REVISE（Guardian (a)×1・Pragmatist APPROVE・Skeptic (a)×4）。P0 生数 20 → 11 → 9 → **6**、(b) は 3 ラウンド連続ゼロ。6 件は 3 根: I.「判定を運んだ行」の一意性と無申告の狭窄（×3）／II. 完結述語×受理の門の体数下限（×2、codex の 1 件も同根）／III. 差し戻しの粒度（×1）

## 1. 修正（3 件・上限内）と、各修正が新しく主張すること

| # | 修正 | この修正が新しく主張すること |
|---|---|---|
| J1 | 実質の測り方（INV-R2） | 座席の実質は提出の全体で測る — persona 座席は行の集合（いずれかの行が実質を伴えば座席は実質）。現行の実質判定と同じ広さで、狭めも広げもしないことを明記。「判定を運んだ行」の選択問題は消滅 |
| J2 | 受理と確定（INV-R4） | collect の成立 = 呼び出しの受理（循環の除去: 受理が記録の構成を生む）。無応答の確定は受理の時点（期限は再走の幅を区切るだけ）。現行の体数下限（2 体未満を受けない）の変更要求を条文内で申告 |
| J3 | 差し戻しの粒度（INV-R4） | 粒度（行ごとか呼び出し全体か）は機構で backlog（現行 = 呼び出し全体）。どの粒度でも差し戻しの事実と事由の可読性は変わらない |

付随: §6 backlog に 2 項目追加（体数下限の扱い／差し戻しの粒度）、痕跡の項目に書き込み点を追記（現行は非同期経路のみ投函時に印を書く — Pragmatist R4 (c) の反映）。

## 2. R5 で advisory（(c)・queue）扱いのもの

R4 までの (c) を引き継ぐ（承継の地図の完全性／INV-R6 の粒度／導出規則の名指し／「事由」欄の札と語形／空 findings 形の実質判定と INV-R5 型の併記スロット／INV-R1 と結び段落の座席語彙化／INV-R4 と R7 の言い回し競合／痕跡の弱化の明示 等）。宣言済み Open Unknown 2 件（批准 2026-07-31）の再指摘は (c)。空返答型 APPROVE は数えない。

## 3. 合格条件・閉じ方（変更なし）

土台 spec §2〜§4 のとおり: (a) ゼロ＋(b) 消尽で合格、(c) は advisory。閾値 3/5 は参考値、主経路は (a)+(b) 消尽 → masaomi の freeze 宣言。配布は枠ごと（claude_cli = 本文込み）。collect 期限 5400 秒（override 明示）。反証専任を投函前に 1 体。
