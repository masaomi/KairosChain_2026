# AUD-L2 mutual anchoring — R3 disposition (v0.3 → v0.4)

Round: R3, 2026-07-21. Verdict: 4/6 APPROVE (cli4.6, cli4.8, cursor, persona team 2/2) — threshold reached, but (a)-class P1s remained in codex REJECT×2 + cross-cited by approvers, so revise continued per project rule (any (a)+(b) blocking finding → revise). All 6 R2 residual causes (RC1'-RC6') confirmed RESOLVED by both personas.

Per-reviewer class breakdown:
- claude_cli_opus4.6: APPROVE. P2 (a): MAP-2/MAP-4 retraction cross-reference missing. P3 advisory.
- claude_cli_opus4.8: APPROVE. P1 (a)×2: observation type non-normative while MAP-4 mandates typing; retraction of head inscriptions not bounded. P2/P3 advisory.
- codex_gpt5.4: REJECT. P1 (a): MAP-3 declared-rule commitment/integrity/anteriority unsecured.
- codex_gpt5.5: REJECT. P1 (a): conforming MAP-1 anchors have no normative type to declare.
- cursor_composer2.5: APPROVE. P2 (a): retract-then-redesignate voids first-in-force; declared-rule anteriority.
- persona transparency-log-architect: APPROVE. P2 (c): retraction-of-designation interaction to make explicit.
- persona adversarial-auditor: APPROVE. P1 (a): MAP-2 justification "second designation changes nothing" falsified by retract-then-redesignate; P2 (a): retraction authority unstated.

Residual causes (converged) → v0.4 fixes:
RC1'' Succession × retraction interaction: "non-retracted" exemption + unrestricted retraction defeats first-wins via compromised key; "in force" used in two senses; no cross-ref. → MAP-2: "credential authority subsisted" (commit-time sense) vs "earliest designation not retracted by its own issuer governs" (standing sense); retraction identified as the MAP-4 typed act, issuer-only; precedence defeasible by the credential holder and no one else; retract-and-redesignate = append-visible trail inside disclosed compromise limit. Justification corrected: attacker cannot SILENTLY override (must retract first, leaving contested trail).
RC2'' Observation type non-normative → MAP-4 names three normative types (observation, quality-endorsement, retraction), each invariant-forced; §11 candidate list adjusted.
RC3'' Retraction unbounded (who may retract; can it un-anchor) → issuer-only authority as property; retraction withdraws a claim, never removes an entry, never subtracts anchored coverage (retracted observation still anchors; AHM-4).
RC4'' MAP-3 declared-rule anteriority/integrity/decidability → property extended: declaration is a committed artifact; anteriority decidable in committed order (MPR-8); rule applicable from committed state without operator judgment at anchor time; venue/coverage stay §11.
RC5'' (P3 batch) §3(e)/§3(d) harmonization ("different type on the same append substrate"); "prefers" → "no external re-attribution is recognized"; §3(e) term wired into MAP-3 ("the anchoring discipline of §3(e)").

Advisory (c)/P3 not adopted: inherited docs not bundled with artifact (multi-document lineage is the method); retraction fixing two axis positions (type-level taxonomy = §11); §2/§5 AUD-L1.5 dual mention (scope vs growth-path roles differ); provenance blur §1 MPR vs ANC residual attribution (both frozen docs state the residual; traceability reference only).
