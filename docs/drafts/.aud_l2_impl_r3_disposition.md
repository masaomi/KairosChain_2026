# AUD-L2 impl (MAP slice 1) — R3 disposition

Round: R3, 2026-07-21. Verdict: 2/5 APPROVE (cli4.8, persona team 2/2 [prober + invariant-auditor both APPROVE]; REJECT cli4.6, codex5.4, codex5.5; cursor SKIP auth). All R2 clusters RC9-RC11 + adopted P2s confirmed RESOLVED by both personas (probed, incl. triple design↔convention↔code agreement sweep and MAP-1..4 completeness sweep — no undisclosed unrealized statement).

Residual causes (all micro) → fixes (suites after: map 75/75, head_anchor 76, synoptis 88, constitutive 35+25+17; mirror OK, template-side green):
RC12 [codex×2 P1] stray target_record_sha256 alongside valid target_entry_hash still appendable ("ONLY target form" letter) → intake rejects the stray key outright; test added.
RC13 [codex5.5 P1 INFERRED, cli4.6/4.8 P2] at-or-before marking relied on scan order (provably correct, prober-probed) → explicit `d[:position] <= position` predicate added with comment noting redundancy-by-design.
RC14 [cli4.8 P2] retraction-of-a-retraction not checked in anchor-log coherence → retraction_coherence flags target with attestation_type=retraction ("not recognized, map-1 §3"); test added.
RC15 P3 batch: contested sort keyed [position, reason] (deterministic under hypothetical ties); strict_int digits-only regex (refuses '1_0', whitespace); retraction subcommand target_ref value shape check (String + 64-hex); position-order coherence check now mismatches when positions are not both Integers (fabricated log cannot pass by omitting the field).
Not adopted (advisory (c), personas' own residuals): library Rule.coverage base-implicit Integer for zero-padded strings (unreachable via CLI, visible via frame echo); '(retract-and-redesignate trail)' label on orphan-case trails (convention names every retracted designation so; superset of design's phrase, §11 mechanism choice); [R,D] noise leaving empty contested (no designation was ever retracted; documented model).

Reviewer note (observation log): cli4.6 flipped APPROVE→REJECT across R2→R3 on re-litigation of the scan-order predicate (probed-correct); codex pair continues surface-letter findings after semantic resolution — consistent with the established codex (c)-tail characteristic, though RC12 was a legitimate letter-of-convention catch.
