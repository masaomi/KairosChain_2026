# v0.7 R2 レビュー仕様（事前宣言・差分のみ）— persona の座席と事由の非列挙化

- 日付: 2026-07-31
- 状態: DECLARED（R2 投函前に宣言。投函後は変更しない）
- 土台: `docs/drafts/multi_llm_review_v07_review_spec_20260731.md`（対象・合格条件・閉じ方・配布・運用をすべて引き継ぐ）。本書は差分のみ
- R1 の結果: 集計 REVISE（APPROVE 1/4 — claude_cli のみ、raw 3,331 字・実測つき）。P0 20 件は 5 つの根に畳めた: A. persona の座席と母数（persona 合成行の指摘の大半。当日の persona 3 体の個別出力に基づくが、個別出力は現行記録に残らない — それ自体が INV-R3 の動機）／B. INV-R2 の範囲宣言（§5 委任の超過が未申告・「完結」列挙の矛盾・結論の位置の条文欠落 (b)）／C. 事由の列挙が自壊（実質なし・excluded_slots・escalation・配布不能の 4 漏れ）／D. 食い違い票の両様（INV-R4 と R5）／E. INV-P1 位置分離が括弧内主張 (b)。masaomi の裁定（2026-07-31）: この 5 束で R2 へ

## 1. 修正（5 件・上限ちょうど）と、各修正が新しく主張すること

| # | 修正 | この修正が新しく主張すること |
|---|---|---|
| F1 | INV-R3 全面改稿（persona の座席と行） | persona 集合（体数＋各自の申告モデル）は投函時に申告され記録に入る。行は記録の粒度で座席は INV-P2 のまま一つ。申告由来の名の位置分離（INV-P1）を条文として引き継ぐ。座席判定の導出は記録から読める（規則の選択は backlog・現行優先順位が既定） |
| F2 | INV-R1 帰趨文＋INV-R4 全面改稿（経路の終局性と事由の非列挙） | 語彙外提出の帰趨は言い直せるかで分岐（外部＝終局・判定なし、persona＝差し戻し）。事由の類型は列挙しない。記録は collect 成立時点で構成され、その時点で提出の無い申告済み persona が無応答。成立しない実行は pending がその記録 |
| F3 | INV-R2 全面改稿（参考値化と結論の不在） | 比率は結論の位置を占めない（(b) の直接の直し）。INV-E2 の母数規則は数を参考値として存続。§5 委任の一歩外（届いた閾値も座を降りる）に及ぶことを条文内で申告 |
| F4 | INV-R5 改稿（食い違いの一本化） | 食い違い票は母数にとどまり除外集計を併記。食い違いは母数を動かす事由ではない（INV-R4 の側からも食い違いを除去） |
| F5 | INV-R7 改稿（配布属性の観測者化） | 配布形式は記録上の観測者ごとの属性（正本枠に限らない — 独立観測者の persona も覆う）。配布できる形式が尽きた観測者は招集されず、事由が記録に残る |

付随（修正ではない）: §6 backlog に 3 項目追加（座席判定の導出規則／診断欄の中身の格／配布属性の解決のしかた）。位置づけ（ヘッダ）の v0.6 関係文を F1/F3 と整合する形に改稿。

## 2. R2 で advisory（(c)・queue）扱いのもの

時制・活用の範囲の具体（backlog）／api_error_status の中身の格（backlog に追加済み）／到達可能性の算定時点（backlog）／「事由」「語形」の語義注記／INV-R7 の執行点／codex [INFERRED] 2 件（roster の版の pin は F1 の集合申告と F5 の観測者ごと記録で実質吸収、再指摘があれば実測要求）。宣言済み Open Unknown 2 件（§5、批准 2026-07-31）の再指摘は (c)。

## 3. 合格条件・閉じ方（変更なし）

土台 spec §2〜§4 のとおり: (a) ゼロ＋(b) 消尽で合格、(c) は advisory。閾値 3/5 は参考値、主経路は (a)+(b) 消尽 → masaomi の freeze 宣言。配布は枠ごと（claude_cli = 本文込み）。collect 期限 5400 秒（`collect_deadline_seconds_override` で明示）。反証専任を投函前に 1 体。
