# AUD-L2 impl (MAP slice 1) — R2 disposition

Round: R2, 2026-07-21. Verdict: REVISE (1/5 APPROVE cli4.6; REJECT cli4.8, codex5.4, codex5.5; cursor SKIP auth; persona prober APPROVE / invariant-auditor REVISE). All R1 clusters RC1-RC8 + adopted P2s confirmed RESOLVED by both personas (probed) and reviewers.

Converged residual causes → v-next fixes (suites after: map 73/73, head_anchor 76, synoptis 88, constitutive 35+25+17; mirror OK):
RC9 [codex×2 + cli4.8 P1] anchor-log retraction intake accepted target_record_sha256 which no verifier on the surface can resolve → intake now requires metadata.target_entry_hash ONLY; map-1 §3 amended (pre-pin): internal-chain take-backs are §4's record form, never anchor-log entries; refusal test added.
RC10 [persona (b) P1 PROBED] retract-and-redesignate trails absent from contested register (silent narrowing of FROZEN MAP-2 "one and the same register") → governance surfaces every retracted designation as contested {position, reason: retracted at N (retract-and-redesignate trail)}; contested sorted by position; map-1 §4.1 amended to state the register contents ("no path to a governing successor is silent"); test added. Retraction marking scoped to copies at-or-before the retraction (byte-identical later re-designation is a fresh act — matches prober's probe and "later committed position").
RC11 [cli4.8 P1] Integer() octal ('010'→8) silently shifts governance boundary → strict_int uses base 10 explicitly.
Adopted P2/P3: retraction subcommand body/metadata shape guards (no uncaught TypeError); credential JSON must be a Hash at CLI (load_json_hash); governance guards non-Array records (SuccessionError); pair counts only kind=anchor entries; require_binding_wellformed! also checks chain_identity form; coverage report echoes assessed frame (chain_extent/rule_position; rule_moment/assessed_until) so defaulted rule_position is visible; every_n_days usage documents 0 0 placeholders; retraction_coherence adds position-order mismatch; RC1 comment reworded to at-or-before semantics.
Not adopted (advisory (c)): VocabularyError as new exception family (documented, callers opt in); Symbol→String coercion of attestation_type (vocabulary check bounds it); positional-arg ergonomics beyond doc note; .tmp probe files (rm denied by session permissions — remove at commit: .tmp_ed25519_probe.rb, .tmp_aud_l2_r2_probe.rb).
Judged (c) by auditor persona and accepted: map-1 §3 issuer=depositor is a faithful DISCLOSED deferral of MAP-4's credential authority (depositor = ANC-5 authenticated identity; §11 defers retraction-entry construction; disclosure inside the convention text).
