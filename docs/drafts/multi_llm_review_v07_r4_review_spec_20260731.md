# v0.7 R4 レビュー仕様（事前宣言・差分のみ）— 座席語彙の完成

- 日付: 2026-07-31
- 状態: DECLARED（R4 投函前に宣言。投函後は変更しない）
- 土台: `docs/drafts/multi_llm_review_v07_review_spec_20260731.md`＋R2/R3 差分 spec。本書は R3 からの差分のみ
- R3 の結果: 集計 REVISE。実質 APPROVE は claude_cli（2,868 字）と、当日セッション内では persona Pragmatist（個別出力は現行記録に残らない — それ自体が本設計 INV-R3 の動機）。codex は 2 ラウンド連続の 41 文字空返答型 APPROVE（数えない）。P0 生数 20 → 11 → 9 と単調減（R1/R2 の実行記録は保持 3600 秒の掃除で既に消えており、20 と 11 は spec 系列の記録による — この消失自体が本設計の動機の実例）。(b) は 2 ラウンド連続ゼロ。残る (a) 9 件のうち 5 件は支配根 1 つ（G1 の座席化が INV-R3/R4 で止まり、INV-R2 の INV-E2 再掲が返答単位のまま。実質と食い違いの順序未定もここに連なる — 記録上 2 座席が独立指摘）、残片 4 つ（完結述語の未定義／痕跡を書く主体／persona 座席の「配布」未定義／差し戻しの座標が INV-R1 と R4 で逆側）

## 1. 修正（5 件・上限ちょうど）と、各修正が新しく主張すること

| # | 修正 | この修正が新しく主張すること |
|---|---|---|
| H1 | 実質規則の座席化（INV-R2/R4） | INV-E2 の二事由（無応答・実質の不在）を座席語彙で復元。座席の実質は「判定を運んだ提出」（外部=返答、persona=判定を運んだ行）で測る。母数を動かすのは「実質を伴う判定を運ばない座席」 |
| H2 | 完結述語＋痕跡の起点（INV-R4） | collect の成立 = 最終記録の構成（全員が揃うことは要件でない）。実行の存在の印は投函時に書かれ、完結の記録が置き換える — 痕跡を書く主体を期限の観測に頼らせない |
| H3 | 座席語彙の全条文掃引（INV-R5/R7＋ヘッダ） | INV-R5・R7 を座席語彙に統一。INV-R5 の適用は観測から実モデルを取り出せる座席のみ（INV-E5 準拠、persona 行の食い違いは定義されない）。ヘッダに INV-E4/E5 の存続宣言を追加 |
| H4 | 差し戻しの座標（INV-R1/R4） | 差し戻しは受理の門での系の観測（呼び出し元の申告ではない）。INV-R4 の「提出前」を「受理の門で」に統一 |
| H5 | persona 座席と配布（INV-R7） | 配布 = 系が成果物を座席へ運ぶ行為。harness 内で原本を参照する座席には配布の行為が存在せず、INV-R7 は適用されない |

## 2. R4 で advisory（(c)・queue）扱いのもの

R3 までの (c)/P1 を引き継ぐ: 座席印の欄・導出規則の名指し・事由欄の札と語形の分離・無提出成立経路・結論欄の「注記」選択肢の適否・「加えて一箇所」の数え方・INV-R3 の申告リストの体裁・最小痕跡と INV-R6 の束ね（cursor R3 P1 — 実装時に痕跡へ spec 参照を含めるかは backlog「痕跡の形」の中で決める）。宣言済み Open Unknown 2 件（批准 07-31）の再指摘は (c)。空返答型 APPROVE は数えない。

## 3. 合格条件・閉じ方（変更なし）

土台 spec §2〜§4 のとおり: (a) ゼロ＋(b) 消尽で合格、(c) は advisory。閾値 3/5 は参考値、主経路は (a)+(b) 消尽 → masaomi の freeze 宣言。配布は枠ごと（claude_cli = 本文込み）。collect 期限 5400 秒（override 明示）。反証専任を投函前に 1 体。
