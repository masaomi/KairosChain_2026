# AUD-L2 impl (MAP slice 1) — R4 disposition (CONVERGED)

Round: R4, 2026-07-21. Verdict: **APPROVE — 5/5** (cli4.6, cli4.8, codex5.4, codex5.5, persona team 2/2; cursor SKIP auth error all 4 rounds). Tool consensus verdict: APPROVE. P0/P1 (a)/(b) remaining: ZERO.

RC12-RC15 confirmed RESOLVED by both execution personas (probed: stray-key refusal incl. symbol-key smuggling; at-or-before predicate semantics unchanged on real signed records; retraction-of-retraction incoherent library+CLI; sort determinism; six malformed-integer and two malformed-target probes all clean). Invariant-auditor final sweep: no design↔convention↔code divergence; dispositioned-(c) items unworsened; map-1.md digest equal on live and template copies (25feb445…8978); "freeze-worthy for the human commit gate".

Residual advisory (P2/P3, not blocking, note for future polish): retraction_coherence mismatch messages could interpolate both depositor values (artifact showed ellipsis, code does interpolate — presentation artifact); stray-key rule enforced at intake only (coherence reads target_entry_hash only — read-side unaffected); symbol/string key collapse last-writer-wins in transform_keys (intake normalization, bounded); position semantics `<=` (coherence, entries strictly ordered) vs `<=` at-or-before (succession records) are different surfaces by design; strict_int upper bound unbounded (CLI use bounded by JSON_SAFE_BOUND downstream).

Final state: test_map_anchoring 75/75; head_anchor 76/76; synoptis 88/88; constitutive 35+25+17. Template mirror byte-identical. Trajectory R1 0/5 → R2 1/5 → R3 2/5 → R4 5/5.

Reviewer characteristics (observation log): execution-probing personas carried the round-over-round signal (every blocking finding in R1-R3 was PROBED before being fixed; refuted plausible-but-wrong claims each round — e.g. symbol-key digest nondeterminism, DST arithmetic, canonical-json round-trip). cli4.6 flip-flopped APPROVE→REJECT→APPROVE (R2→R3→R4) on the scan-order predicate re-litigation. codex pair produced one genuine letter-of-convention catch (RC12) amid surface re-litigations, and both returned bare 42-byte APPROVE on convergence. cursor absent all rounds (auth) — roster effectively 5+persona this cycle.

Remaining (human gates, in order): remove probe strays (.tmp_ed25519_probe.rb, .tmp_aud_l2_r2_probe.rb; rm denied to session) → commit (live .kairos repo + template repo) → gem version bump (3.44.0 → 3.45.0) + release → server deploy → inaugural mutual anchor REQUIRES a second HestiaChain instance under another operator (map-1 §11-equivalent: observation log feeds deferred trust composition).
