# AUD-L2 impl (MAP slice 1) — R1 disposition

Round: R1, 2026-07-21. Verdict: REVISE (0/5 APPROVE; REJECT cli4.6, cli4.8, codex5.4, codex5.5; cursor SKIP auth error; persona 2/2 REVISE, both execution-probing).

Converged blocking clusters → fixes (all mirrored, live+template suites green: map 71/71, head_anchor 76, synoptis 88, constitutive 35+25+17):
RC1 [PROBED] duplicate designations defeat retraction (find→first only) → mark ALL designations sharing the targeted digest (succession.rb).
RC2 [PROBED] changeover boundary `>=` vs convention "at or before" → code moved to `position > changeover`; boundary test added; old test scenario re-pinned at changeover 0.
RC3 [PROBED] every_n_days unverifiable via CLI + capability gap mislabeled REJECTED → coverage subcommand accepts [rule_moment] [now]; missing required inputs = usage exit 2; every_n_days CLI test added.
RC4 [PROBED] CLI to_i coercion ('abc'→0→vacuous covered) → strict_int with die (exit 2); library coverage_by_records wraps Integer() failures into RuleError.
RC5 [PROBED] attestation_type dead parameter (unreachable from real write surface) → wired through Log#append_anchor and WritePath#deposit with intake validation; test proves reachability through the real append path.
RC6 [PROBED] map-1 §3 anchor-log retraction promise had no code → new module attestation_types.rb (vocabulary intake validation; retraction target reference required; retraction_coherence diagnostic = target match + depositor equality); map-1.md §3 amended (pre-pin) to disclose issuer=depositor at map-1, credential-level binding deferred (design §11); map_verify gains `retraction` subcommand.
RC7 codex5.5 [P1] non-canonical/extra-field succession records accepted (digest splitting) → parse_record enforces canonical serialization; exact field sets enforced; map-1.md §4 states validity-only-in-canonical-form.
RC8 pair grep-only (4.8/codex5.4) → matched entries now checked: binding well-formedness (khab-1 shapes; malformed = exit 2) + entry_hash recompute (mismatch = REJECT); output CONDITIONS states the residual scope (full chain = khab_verify's business); tamper + malformed tests added.
Adopted P2s: rule canonical-form check (one rule one digest); attestation payload String-only; governance changeover_position strict (SuccessionError, non-negative); succession CLI requires array of strings; every_n_days library tests; map-1.md §2/§5 literals shown in canonical (sorted) order.
Not adopted (advisory (c)): conforms:true informational-by-design (MAP-3: adequacy priced not legislated); constant dedup across modules; memoization race under alternative runtimes; negative-changeover CLI guard (added anyway at library level); verify_raw rescue breadth (raw-length guard bounds it).

map-1.md edits legitimacy: convention not yet committed/pinned anywhere in production (no anchors, no deposits reference its digest) — pre-release amendment, not in-place edit of a committed convention. Code/test/convention now agree; the digest pinned by ChainCredential.convention_sha256 is recomputed from the shipped file at load.
