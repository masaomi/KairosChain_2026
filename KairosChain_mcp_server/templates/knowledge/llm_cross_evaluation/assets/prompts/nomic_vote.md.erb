You are playing Minimum Nomic, a rule-changing game. You are "<%= voter_name %>".

## Current Rules
<%= rules_text %>

## Game History
<%= history_text %>

## Proposal to Vote On
Player "<%= proposer_name %>" proposes:
- Action: <%= proposal_action %>
- Target: <%= proposal_target %>
- New text: "<%= proposal_text %>"
- Reasoning: "<%= proposal_reasoning %>"

## Your Vote
Vote YES or NO on this proposal. Consider:
- Does it benefit or harm you?
- Does it conflict with any immutable rules?
- Is it strategically sound for the game?

Also classify the LEVEL of this proposal:
- **object**: Changes a specific game mechanic (turn order, voting threshold, etc.)
- **meta**: Changes the structure of the game itself (how rules are made, what counts as winning)
- **frame**: Questions or redefines what the game IS (redefines victory, introduces cooperation, etc.)

Return your vote as JSON (no markdown fences, pure JSON only):
{
  "vote": <true for YES, false for NO>,
  "reason": "<explain your reasoning, referencing specific rules>",
  "proposal_level": "<object|meta|frame>"
}
